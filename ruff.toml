# Ruff configuration — mirrors [tool.ruff] in pyproject.toml
# This file is kept for editors that look for ruff.toml at the project root.

target-version = "py311"
line-length = 99

[lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[lint.isort]
known-first-party = ["src"]
